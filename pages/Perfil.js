import { ScrollView } from "react-native";
import { AvatarImage } from "../components";

const Perfil = () => {

  return (
    <ScrollView>
      <AvatarImage />
    </ScrollView>
  );

};

export default Perfil;